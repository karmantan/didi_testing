# Pinned versions for the didi_workbench_bundle environment.
#
# The two HARD pins (never change these without re-validating the whole bundle) are
# Python 3.9.1 (see bundle_settings.env PYTHON_CANDIDATES) and polars==0.20.16. Both come
# from export_20260911/20pct/260909/run_manifest.json ("target_runtime": {"python":
# "3.9.1", "polars": "0.20.16"}), confirmed in export_20260911/20pct/260909/pipeline.log
# ("Runtime: Python=3.9.1; Polars=0.20.16; collect=streaming=True; ...") -- this is the
# exact environment the real 20% run and the Gate 2 1% validation both used.
#
# numpy/pandas/scikit-learn/scipy/pyarrow/matplotlib/PyYAML versions come from the Gate 2
# environment assertions described in export_20260911/GATE2_REPORT.md ("Repeated
# environment assertions passed: Python 3.9.1, Polars 0.20.16, NumPy 1.25.0, pandas 2.3.3,
# scikit-learn 1.5.2, SciPy 1.13.1, PyArrow 15.0.2, Matplotlib 3.8.4, PyYAML 6.0.2").
#
# psutil is added for scripts/monitor_memory.py and scripts/check_machine.py; it was not
# part of the Gate 2 assertions because Gate 2 sampled memory from the container/host
# directly rather than via psutil.
polars==0.20.16
numpy==1.25.0
pandas==2.3.3
scikit-learn==1.5.2
scipy==1.13.1
pyarrow==15.0.2
matplotlib==3.8.4
PyYAML==6.0.2
psutil>=5.9
